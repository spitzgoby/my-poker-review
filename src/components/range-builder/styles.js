import {themeColors} from 'styles/colors'

export const styles = {
  rangebuilder: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%'
  },

  comborow: {
    display: 'flex',
    flexDirection: 'row',
  }
}