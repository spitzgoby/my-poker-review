import {themeColors} from 'styles/colors'

const {
  lightText
} = themeColors

export default {
  button: {
    color: lightText
  },

  snackbar: {
    color: lightText
  },

  wrapper: {
    display: 'inline-block'
  }
}